"use client";

import { useEffect } from "react";

/**
 * ViewportLock preserves natural zoom in/out flexibility while
 * preventing unwanted horizontal layout drift.
 */
export function ViewportLock() {
  useEffect(() => {
    // Allows natural user zooming while maintaining rock-solid layout integrity
  }, []);

  return null;
}
